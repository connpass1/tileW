export default function Layout({
    children 
}: {
    children: React.ReactNode,
    
       
}) {
    return <div className=" px-3  flex flex-col row-start-3  col-span-2 col-start-1" > articles Layout {children}</div>
} 